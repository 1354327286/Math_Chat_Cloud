interface:
  display_name: "Lean Formalization"
  short_description: "Contract, audit, and verify exact mathematics in Lean"
  default_prompt: "Use $lean-formalization. Before any plan write, Lean run, setup, or delegation, ask me to fix the exact target, acceptance/provenance profile, goal mode, subagent permission plus model/reasoning limits, and toolchain authorization."
policy:
  allow_implicit_invocation: false
